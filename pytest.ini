[pytest]
pythonpath = agents
testpaths = tests agents/tests
python_files = test_*.py
markers =
    regression: deterministic regression coverage for the multi-agent pipeline
    unit: fast unit tests for helper and schema logic
